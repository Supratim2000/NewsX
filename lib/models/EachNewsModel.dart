class EachNewsModel {
    String urlToImage;
    String title;
    String description;
    String content;
    String url;

    EachNewsModel ({
        required this.urlToImage,
        required this.title,
        required this.description,
        required this.content,
        required this.url
    });
}